{
  "name": "gasless",
  "version": "0.1.0",
  "description": "Proof-of-concept for gasless transactions using Flashbots",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "author": "Egor Polyakov <epolyakov@via.exchange>",
  "license": "MIT",
  "devDependencies": {
    "@flashbots/ethers-provider-bundle": "^0.5.0",
    "@typechain/ethers-v5": "^10.1.1",
    "@types/node": "^18.11.9",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.3"
  }
}
